[**@lancedb/lancedb**](../README.md) • **Docs**

***

[@lancedb/lancedb](../globals.md) / BlobUri

# Type Alias: BlobUri

```ts
type BlobUri: string | URL;
```

A URI accepted for a blob value. A `URL` is stored as its `href`.
